# VeilPay demo narration

The narration is generated in one continuous pass from `NARRATION.txt`. Scene timing and subtitles are aligned from the resulting Whisper timestamps, not estimated from word count.

The script contains seven beats: hook, privacy boundary, Nox integration, real Sepolia proof, public result, engineering proof, and close.
